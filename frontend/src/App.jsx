// App.jsx — printdash-only shell.
// The Gridfinity/Vase design studio was removed per user request; only the
// printer-farm dashboard is kept. The default export still exists so
// src/main.jsx doesn't need to change.
import Dashboard from './Dashboard.jsx'

export default function App() {
  return <Dashboard />
}